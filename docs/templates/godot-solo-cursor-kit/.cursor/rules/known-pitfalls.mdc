---
description: 既往エラー／落とし穴の全カタログ（按需）。起動即死の常時要約は pitfalls-startup.mdc
alwaysApply: false
---

# 既往エラー・落とし穴

一度発生し修正済みの事故。同じパターンを繰り返さない。詳細規約は各領域ルールへ。

**常時注入:** 起動・パース即死のみ → `pitfalls-startup.mdc`  
**本ファイル:** 按需。昇格済みクラスタはポインタ1行に圧縮（詳細を二重メンテしない）。

## 書き方

| 症状 | 原因 | 再発防止 |
|---|---|---|
| （例）続きから即終了 | Save キーだけ残してフィールドを切り戻した | GameState＋Save **同コミット** |

## GDScript / 起動

| 症状 | 原因 | 再発防止 |
|---|---|---|
| （追記していく） | | |

## UI / 演出

| 症状 | 原因 | 再発防止 |
|---|---|---|
| （追記していく） | | |

## プレイヤー文言 / オミット

| 症状 | 原因 | 再発防止 |
|---|---|---|
| 手引きが旧仕様のまま | フラグや BAL 変更に文書が追随しない | `player-copy.mdc`。同期テストを足す |

## 禁止

- `alwaysApply: true` に戻して肥大化させること
- 抽象禁止だけ書いて具体例を残さないこと
