# 05_実装ロードマップ 詳細版 v2.1

## 方針

Cursor Impl セッションには、必ず Task 単位で実装を依頼する。

1回の依頼は原則として1〜5Taskまで。
大きな機能を一度に依頼しない。

---

# Phase 0：プロジェクト初期化

## Task001：Godotプロジェクト作成

### 目的
Godot 4.xでCrownfallの空プロジェクトを作成する。

### 実装内容
- Godot 4.xプロジェクトを作成
- メインシーンをBootSceneに設定
- 最低限起動できる状態にする

### 対象ファイル
```text
project.godot
scenes/boot/BootScene.tscn
scripts/boot/BootScene.gd
```

### 完了条件
- プロジェクトがエラーなく起動する
- BootSceneが表示される

---

## Task002：基本ディレクトリ作成

### 目的
今後の実装に必要なフォルダ構造を作る。

### 実装内容
- scenes/
- scripts/
- resources/
- assets/
- saves/

を作成する。

### 対象ファイル
```text
scenes/
scripts/
resources/
assets/
saves/
```

### 完了条件
- implementation/02_ディレクトリ構成.md に沿ったフォルダがある

---

## Task003：Autoload設定

### 目的
ゲーム全体で使うシングルトンを設定する。

### 実装内容
以下をAutoload登録する。

- GameState
- DataRegistry
- SceneRouter
- EventBus

### 対象ファイル
```text
scripts/autoload/GameState.gd
scripts/autoload/DataRegistry.gd
scripts/autoload/SceneRouter.gd
scripts/autoload/EventBus.gd
project.godot
```

### 完了条件
- 各Autoloadが起動時にエラーなくロードされる
- BootSceneからGameStateへアクセスできる

---

# Phase 1：画面遷移

## Task004：BaseScene作成

### 目的
拠点画面の土台を作る。

### 実装内容
- BaseScene.tscn作成
- 仮UIとして「ダンジョンへ」ボタンを配置
- BaseScene.gd作成

### 対象ファイル
```text
scenes/base/BaseScene.tscn
scripts/base/BaseScene.gd
```

### 完了条件
- BaseScene単体で表示できる
- ボタンが見える

---

## Task005：DungeonScene作成

### 目的
探索画面の土台を作る。

### 実装内容
- DungeonScene.tscn作成
- 仮UIとしてダンジョン名、部屋番号、ログ表示を配置
- DungeonScene.gd作成

### 対象ファイル
```text
scenes/dungeon/DungeonScene.tscn
scripts/dungeon/DungeonScene.gd
```

### 完了条件
- DungeonScene単体で表示できる
- ダミーテキストが見える

---

## Task006：ResultScene作成

### 目的
探索終了画面の土台を作る。

### 実装内容
- ResultScene.tscn作成
- 獲得報酬表示用の仮UI
- 「拠点へ戻る」ボタン

### 対象ファイル
```text
scenes/result/ResultScene.tscn
scripts/result/ResultScene.gd
```

### 完了条件
- ResultScene単体で表示できる

---

## Task007：SceneRouterによる画面遷移

### 目的
Boot → Base → Dungeon → Result → Base の流れを作る。

### 実装内容
- SceneRouter.change_scene() 実装
- BootScene起動後BaseSceneへ遷移
- BaseSceneのボタンでDungeonSceneへ
- DungeonSceneの仮ボタンでResultSceneへ
- ResultSceneのボタンでBaseSceneへ

### 対象ファイル
```text
scripts/autoload/SceneRouter.gd
scripts/boot/BootScene.gd
scripts/base/BaseScene.gd
scripts/dungeon/DungeonScene.gd
scripts/result/ResultScene.gd
```

### 完了条件
- 一連の画面遷移がエラーなく動く

---

# Phase 2：データ基盤

## Task008：EnumsとConstants作成

### 目的
ゲーム内の定数・列挙型を統一管理する。

### 実装内容
- Rarity
- ItemType
- WeaponType
- RoomType
- ExplorationPolicy
- TargetPriority

を定義する。

### 対象ファイル
```text
scripts/core/Enums.gd
scripts/core/Constants.gd
```

### 完了条件
- 他スクリプトから参照できる
- 文字列直書きを減らせる

---

## Task009：Stats Resource作成

### 目的
キャラクター・敵のステータス表現を作る。

### 実装内容
- hp
- attack
- defense
- attack_speed
- crit_rate
- crit_damage
- discovery

を持つStatsを作成。

### 対象ファイル
```text
scripts/domain/Stats.gd
```

### 完了条件
- Resourceとして作成可能
- duplicateして使える

---

## Task010：Adventurer Resource作成

### 目的
冒険者データを表現する。

### 実装内容
- id
- display_name
- level
- job_id
- base_stats
- equipped_weapon
- equipped_armor
- equipped_accessory
- traits

を持つ。

### 対象ファイル
```text
scripts/domain/Adventurer.gd
```

### 完了条件
- GameStateに3人分保持できる

---

## Task011：初期パーティ生成

### 目的
ゲーム開始時に3人パーティを作る。

### 実装内容
- 戦士
- 盗賊
- 魔術師

の仮パーティをGameStateに生成する。

### 対象ファイル
```text
scripts/autoload/GameState.gd
scripts/domain/Adventurer.gd
scripts/domain/Stats.gd
```

### 完了条件
- BaseSceneで3人の名前を表示できる

---

# Phase 3：ダンジョン進行

## Task012：DungeonData Resource作成

### 目的
ダンジョン定義をResource化する。

### 実装内容
- id
- display_name
- difficulty
- room_count
- enemy_pool
- boss_id
- drop_table_id
- discovery_unlocks

を持つ。

### 対象ファイル
```text
scripts/data/DungeonData.gd
resources/dungeons/
```

### 完了条件
- 王都跡のResourceを作れる

---

## Task013：王都跡データ作成

### 目的
MVP最初のダンジョンをデータ化する。

### 実装内容
- royal_ruins.tres
- room_count = 10
- boss_id = royal_guard_captain

### 対象ファイル
```text
resources/dungeons/royal_ruins.tres
```

### 完了条件
- DataRegistryから取得できる

---

## Task014：DungeonController作成

### 目的
ダンジョン進行を管理する。

### 実装内容
- current_room_index
- current_room_type
- start_dungeon()
- advance_room()
- finish_dungeon()

を実装。

### 対象ファイル
```text
scripts/dungeon/DungeonController.gd
scenes/dungeon/DungeonScene.tscn
```

### 完了条件
- 10部屋を順番に進める

---

## Task015：部屋タイプ進行

### 目的
固定10部屋構成を実装する。

### 実装内容
```text
START
COMBAT
EVENT
TREASURE
ELITE
EVENT
COMBAT
MID_BOSS
BOSS
EXIT
```

### 対象ファイル
```text
scripts/dungeon/DungeonController.gd
scripts/core/Enums.gd
```

### 完了条件
- 現在部屋タイプがUIに表示される

---

## Task016：方針変更ボタン — **Deferred**（P3-D024a）

### 状態

**未実装 — Backlog（Phase 2）。** Alpha は準備専用（拠点・帰還後の意思決定）。`28_ゲームデザイン点検.md` / `04_ゲームループ.md` を正とする。

### 旧 Task 内容（参考 — 再設計予定）

旧 4 方針（探索 / 戦闘 / 収集 / 帰還）は **最小 2〜3 種**（集中 / 安定 / 撤退）に統合して Task 化する。

### 再 Task 化時の完了条件（案）

- 部屋間 or 戦闘前に方針切替
- ログに方針変更が出る
- `DungeonController` に方針 state 接続

---

# Phase 4：戦闘基盤

## Task017：EnemyData Resource作成

### 目的
敵データをResource化する。

### 実装内容
- id
- display_name
- stats
- ai_type
- skills
- drop_table_id

### 対象ファイル
```text
scripts/data/EnemyData.gd
resources/enemies/
```

### 完了条件
- 亡国兵データを作成できる

---

## Task018：EnemyUnit作成

### 目的
画面上の敵Unitを作る。

### 実装内容
- EnemyUnit.tscn
- HP表示
- EnemyDataを受け取る
- 現在HPを持つ

### 対象ファイル
```text
scenes/dungeon/EnemyUnit.tscn
scripts/combat/EnemyUnit.gd
```

### 完了条件
- DungeonSceneに敵を表示できる

---

## Task019：CharacterUnit作成

### 目的
画面上の味方Unitを作る。

### 実装内容
- CharacterUnit.tscn
- HP表示
- Adventurerを受け取る
- 現在HPを持つ

### 対象ファイル
```text
scenes/dungeon/CharacterUnit.tscn
scripts/combat/CharacterUnit.gd
```

### 完了条件
- DungeonSceneに3人表示できる

---

## Task020：CombatController作成

### 目的
自動戦闘を統括する。

### 実装内容
- 味方リスト
- 敵リスト
- 戦闘開始
- 戦闘終了判定

### 対象ファイル
```text
scripts/combat/CombatController.gd
```

### 完了条件
- COMBAT部屋で戦闘が開始する

---

## Task021：通常攻撃

### 目的
自動通常攻撃を実装する。

### 実装内容
- attack_speedに応じて攻撃
- 最寄り敵を対象にする
- ダメージを与える

### 対象ファイル
```text
scripts/combat/CombatController.gd
scripts/combat/DamageCalculator.gd
scripts/combat/TargetSelector.gd
```

### 完了条件
- 味方が自動で敵を攻撃する
- 敵HPが減る

---

## Task022：死亡・部屋クリア

### 目的
戦闘終了を処理する。

### 実装内容
- HP0で死亡
- 敵全滅で部屋クリア
- 味方全滅で探索失敗

### 対象ファイル
```text
scripts/combat/CombatController.gd
scripts/dungeon/DungeonController.gd
```

### 完了条件
- 敵全滅後に次部屋へ進む
- 味方全滅時にResultSceneへ遷移する

---

# Phase 5：スキル

## Task023：SkillData Resource作成

### 目的
スキルをデータ化する。

### 実装内容
- id
- display_name
- cooldown
- trigger_type
- target_type
- power_multiplier
- effect_type

### 対象ファイル
```text
scripts/data/SkillData.gd
resources/skills/
```

### 完了条件
- スキルResourceを作成できる

---

## Task024：SkillExecutor作成

### 目的
スキルのクールタイムと発動を管理する。

### 実装内容
- cooldown管理
- can_cast()
- cast()

### 対象ファイル
```text
scripts/combat/SkillExecutor.gd
```

### 完了条件
- 一定時間ごとにスキルが発動する

---

## Task025：MVPジョブスキル実装

### 目的
5ジョブの代表スキルを実装する。

### 実装内容
- 旋風斬り
- 挑発
- 急所突き
- 三連射
- 魔力弾

### 対象ファイル
```text
resources/skills/
scripts/combat/SkillExecutor.gd
```

### 完了条件
- 5種類のスキルが戦闘中に発動する

---

## Task026：武器固有スキルの入口

### 目的
武器に紐づく固有スキルを発動できるようにする。

### 実装内容
- WeaponData.fixed_skill_id
- 装備武器からスキルを取得
- SkillExecutorに登録

### 対象ファイル
```text
scripts/data/WeaponData.gd
scripts/domain/WeaponInstance.gd
scripts/combat/SkillExecutor.gd
```

### 完了条件
- 武器を変えると発動スキルが変わる

---

# Phase 6：装備・インベントリ

## Task027：ItemInstance基底作成

### 目的
装備アイテムの基底を作る。

### 実装内容
- instance_id
- item_type
- rarity
- is_identified

### 対象ファイル
```text
scripts/domain/ItemInstance.gd
```

### 完了条件
- WeaponInstanceなどの親として使える

---

## Task028：WeaponData / WeaponInstance実装

### 目的
武器データと個体を分離する。

### 実装内容
- WeaponData
- WeaponInstance
- attackロール
- affixes

### 対象ファイル
```text
scripts/data/WeaponData.gd
scripts/domain/WeaponInstance.gd
resources/weapons/
```

### 完了条件
- 同じWeaponDataから別個体を作れる

---

## Task029：Inventory実装

### 目的
入手アイテムを保持する。

### 実装内容
- GameState.inventory
- add_item()
- remove_item()

### 対象ファイル
```text
scripts/autoload/GameState.gd
```

### 完了条件
- アイテムを追加・削除できる

---

## Task030：装備変更

### 目的
冒険者に武器を装備させる。

### 実装内容
- equip_weapon()
- unequip_weapon()
- ステータス再計算

### 対象ファイル
```text
scripts/domain/Adventurer.gd
scripts/ui/EquipmentUI.gd
```

### 完了条件
- 装備変更で攻撃力が変わる

---

# Phase 7：ドロップ・Affix

## Task031：AffixData / AffixInstance作成

### 目的
ランダムオプションを実装する。

### 実装内容
- AffixData
- AffixInstance
- 数値ロール

### 対象ファイル
```text
scripts/data/AffixData.gd
scripts/domain/AffixInstance.gd
resources/affixes/
```

### 完了条件
- Affix付き武器を生成できる

---

## Task032：DropManager作成

### 目的
ドロップ生成を管理する。

### 実装内容
- generate_drop()
- roll_rarity()
- roll_item_type()
- roll_weapon_base()

### 対象ファイル
```text
scripts/loot/DropManager.gd
```

### 完了条件
- ボス撃破時にランダム武器が出る

---

## Task033：AffixRoller作成

### 目的
レアリティに応じてAffixを付与する。

### 実装内容
- レアリティ別Affix数
- 部位別候補
- 数値ランダム

### 対象ファイル
```text
scripts/loot/AffixRoller.gd
```

### 完了条件
- Rare以上にAffixが付く

---

## Task034：未鑑定状態

### 目的
探索で拾ったアイテムを未鑑定化する。

### 実装内容
- is_identified = false
- 未鑑定名を表示
- 詳細非表示

### 対象ファイル
```text
scripts/domain/ItemInstance.gd
scripts/ui/InventoryUI.gd
```

### 完了条件
- 拾った直後は詳細が見えない

---

# Phase 8：鑑定

## Task035：IdentifyService作成

### 目的
鑑定処理をロジックとして分離する。

### 実装内容
- identify_item()
- is_identified更新
- 鑑定ログ出力

### 対象ファイル
```text
scripts/loot/IdentifyService.gd
```

### 完了条件
- 未鑑定アイテムを鑑定済みにできる

---

## Task036：鑑定UI作成

### 目的
拠点で鑑定できる画面を作る。

### 実装内容
- 未鑑定リスト
- 鑑定ボタン
- 結果表示

### 対象ファイル
```text
scenes/ui/IdentifyPanel.tscn
scripts/ui/IdentifyPanel.gd
```

### 完了条件
- UIから鑑定できる

---

## Task037：Legendary演出

### 目的
レジェンド鑑定時の快感を作る。

### 実装内容
- 画面停止
- 武器名表示
- 固有スキル表示
- フレーバーテキスト表示

### 対象ファイル
```text
scripts/ui/IdentifyPanel.gd
scenes/ui/LegendaryReveal.tscn
```

### 完了条件
- Legendary鑑定時に特別演出が出る

---

# Phase 9：発見度

## Task038：DungeonProgress保存

### 目的
ダンジョンごとの発見度を保存する。

### 実装内容
- discovery
- hidden_room_unlocked
- hidden_boss_unlocked

### 対象ファイル
```text
scripts/autoload/GameState.gd
```

### 完了条件
- ダンジョン別に発見度を持てる

---

## Task039：発見度上昇

### 目的
探索結果で発見度を上げる。

### 実装内容
- 部屋到達
- イベント成功
- ボス撃破

で発見度加算。

### 対象ファイル
```text
scripts/dungeon/DungeonController.gd
```

### 完了条件
- リザルトで発見度が増える

---

## Task040：発見度解放

### 目的
発見度に応じて要素を解放する。

### 実装内容
- 30% 隠し部屋
- 60% 特殊イベント
- 100% 隠しボス

### 対象ファイル
```text
scripts/dungeon/DungeonController.gd
scripts/autoload/GameState.gd
```

### 完了条件
- 発見度条件でログが出る
- 次回探索に反映される

---

# Phase 10：セーブ

## Task041：SaveManager作成

### 目的
ゲーム進行を保存する。

### 実装内容
- save_game()
- load_game()
- JSON保存

### 対象ファイル
```text
scripts/save/SaveManager.gd
```

### 完了条件
- gold, inventory, party, dungeon_progressを保存できる

---

## Task042：起動時ロード

### 目的
Boot時にセーブを読み込む。

### 実装内容
- セーブありならロード
- なしなら初期データ作成

### 対象ファイル
```text
scripts/boot/BootScene.gd
scripts/save/SaveManager.gd
scripts/autoload/GameState.gd
```

### 完了条件
- 再起動後も発見度・装備が残る

---

# Phase 11：MVPダンジョン拡張

## Task043：白骸墓地追加

### 目的
2つ目のダンジョンを追加する。

### 実装内容
- DungeonData
- EnemyData
- DropTable
- 白骸系レジェンド

### 完了条件
- 白骸墓地に出撃できる

---

## Task044：地下工廠追加

### 目的
3つ目のダンジョンを追加する。

### 実装内容
- DungeonData
- EnemyData
- DropTable
- 工廠系レジェンド

### 完了条件
- 地下工廠に出撃できる

---

## Task045：初回レジェンド確定導線

### 目的
初回30分以内にLegendary体験を保証する。

### 実装内容
- 王都跡2〜3周後
- 王の墓所イベント
- 3択レジェンド

候補

- 葬鐘
- 黒陽の槍
- 星落とし

### 完了条件
- 初回プレイ30分以内にLegendaryを入手できる

---

# Phase 12：MVP完成調整

## Task046：5分周回調整

### 目的
通常ダンジョンの周回時間を4〜6分へ調整する。

### 完了条件
- 王都跡が平均4〜6分で終わる

---

## Task047：序盤ドロップ調整

### 目的
序盤の装備更新頻度を調整する。

### 完了条件
- 2〜3周に1回は装備更新が起こる

---

## Task048：3ビルド検証

### 目的
**状態異常・属性弱点・クリティカル** の違いを確認する（P3-D024h）。旧「出血・召喚」は Backlog。

### 完了条件
- 各ビルドで明確な強みがある
- SSOT: `02_MVP設計.md` / `27_状態異常と属性.md`

---

## Task049：UI最低限調整

### 目的
スマホ向けに最低限見やすいUIにする。

### 完了条件
- HP、発見度、ドロップが見える（方針 UI は P3-D024a Backlog）

---

## Task050：MVPプレイテスト

### 目的
MVP完成判定を行う。

### 完了条件
- 30分遊んでゲームループが成立している
- 初回レジェンドが得られる
- もう1周したい感覚がある
